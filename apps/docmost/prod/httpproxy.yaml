apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: docmost-shared
spec:
  virtualhost:
    fqdn: docmost.shared.cedille.club
    tls:
      secretName: docmost-tls
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      services:
        - name: docmost
          port: 3000
---
apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: docmost
spec:
  virtualhost:
    fqdn: docmost.etsmtl.club
    tls:
      secretName: docmost-tls
      minimumProtocolVersion: "1.3"
  routes:
    - conditions:
        - prefix: /
      services:
        - name: docmost
          port: 3000
